export default {
    setToken(state, token) {
        state.token = token;
    },
    setBanner(state, banner) {
        state.banner = banner;
    },
    setCoffeList(state, coffeList) {
        state.coffeList = coffeList
    }
}